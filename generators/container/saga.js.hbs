import { takeLatest, call, put } from 'redux-saga/effects';
import request from '../../utils/request';
import { LOAD_{{ constantCase name }}S } from './constants';
import { load{{ properCase name }}sError, {{ camelCase name }}sLoaded } from './actions';
import normalize{{ properCase name }}s from './normalizr';

export default function* {{ camelCase name }}sData() {
  yield takeLatest(LOAD_{{ constantCase name }}S, getAll{{ properCase name }}s);
}

export function* getAll{{ properCase name }}s() {
  try {
    let {{ camelCase name }}s = yield call(request, 'http://localhost:3004/{{ lowerCase name }}');
    {{ camelCase name }}s = normalize{{ properCase name }}s({{ camelCase name }}s).entities.{{ camelCase name }}s;
    yield put({{ camelCase name }}sLoaded({{ camelCase name }}s));
  } catch (error) {
    yield put(load{{ properCase name }}sError(error));
  }
}